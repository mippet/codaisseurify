<h1>ik ben een form</h1>

<%= form_for [@artist, @artist.songs.new] do |f| %>

  <hr>

  <%= f.label :title %>
<%= f.text_field :title %>

<div class="actions">
      <%= f.submit "Add Artist", class: "btn btn-success" %>
    </div>


<% end %>
